[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader", "npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "php artisan config:clear && php artisan config:cache && php artisan view:clear && php artisan serve --host=0.0.0.0 --port=$PORT"